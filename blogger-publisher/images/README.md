# Recipe image assets

Generated recipe images are stored here before their corresponding queue item is enabled.

Public URL pattern:

`https://raw.githubusercontent.com/blogthisorthat/Family-Meal-Planning-Resources-site/main/blogger-publisher/images/<filename>`

The queue's `imageUrl` and the `<img src="...">` inside `contentHtml` must use the same public URL.
